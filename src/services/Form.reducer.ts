// import student slice and parent slice

/*
  export default function rootReducer(state = {}, action) {
  return {
    student: studentReducer(state.student, action),
    parents: parentsReducer(state.parents, action)
  }
}
  
 */
