interface Props {
  questions: Array<>
  subject: string
}
export default function FormContainer({ questions, subject, form }: Props) {
  // pass questions, subject and form values to input
  // loop through questions and create input field
  // pass activate array to show or hide other input fields
  return <></>
}
